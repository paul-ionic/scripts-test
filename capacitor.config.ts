import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'scripts-test',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
